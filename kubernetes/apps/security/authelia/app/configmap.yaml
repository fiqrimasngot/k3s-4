---
kind: ConfigMap
apiVersion: v1
metadata:
  name: authelia-config-custom
  namespace: security
data:
  users_database.yml: |
    users:
      dfroberg:
        displayname: "Fiqri Masngot"
        password: "${SECRET_AUTHELIA_USER_FIQRI_PASSWORD}"
        email: ${SECRET_AUTHELIA_USER_FIQRI_EMAIL}
        groups:
          - admins
      cfroberg:
        displayname: "Ain Muhajirin"
        password: "${SECRET_AUTHELIA_USER_AIN_PASSWORD}"
        email: ${SECRET_AUTHELIA_USER_AIN_EMAIL}
        groups:
          - users
      cfroberg:
        displayname: "Nurin Iman"
        password: "${SECRET_AUTHELIA_USER_NURIN_PASSWORD}"
        email: ${SECRET_AUTHELIA_USER_NURIN_EMAIL}
        groups:
          - users
      visitor:
        displayname: "Visitor"
        password: "${SECRET_AUTHELIA_USER_VISITOR_PASSWORD}"
        email: ${SECRET_AUTHELIA_USER_VISITOR_EMAIL}
        groups:
          - users
